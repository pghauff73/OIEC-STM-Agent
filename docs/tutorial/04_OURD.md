# OURD

## What you will learn

You will build a problem map containing objects, boundaries, relations, dependencies, exclusions, and unresolved questions.

## Everyday analogy

For a dark kitchen light, the relevant universe may include the bulb, fitting, switch, circuit, breaker, power supply, and the observation conditions. “Replace the bulb” is only one hypothesis inside that universe.

## New vocabulary

**OURD** means Orthogonal Unique Relational Decomposition modeling. A **boundary** states what is inside or outside the problem. A **relation** states how two named things are connected.

## Diagram

The diagram places the kitchen-light problem at the center and connects each relevant object with labeled relations and unresolved edges.

## Command or interaction

Use the tutorial sandbox to reveal one object at a time. Mark any assumption that is not supported by an observation as unresolved.

## Expected output

The result is an inspectable problem map, not a selected implementation. It names what belongs, what does not, and what remains uncertain.

## What just happened?

OURD separated the problem from the first imagined solution. That gives later experiments and actions a stable semantic target.

## Try changing this

Map a parser regression using the parser, grammar, token stream, precedence rules, failing input, expected output, tests, recent changes, and callers.

## Common mistake

Do not turn every nearby object into part of the task. Scope and exclusions are as important as discovered relations.

## Next lesson

Continue to **IURM** and choose one useful variation that can reduce uncertainty.
